{
  "name": "local-background-remover",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "serve:prod": "vite build && python -m uvicorn backend:app --host 127.0.0.1 --port 7000"
  },
  "dependencies": {
    "vite": "^8.0.11"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
